import { Meta, Story } from 'types/StoryBookCustomTypes';
import {{pascalCase name}} from '.';
import {{pascalCase name}}PropsType from './types';

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
} as Meta;

export const Template: Story<{{pascalCase name}}PropsType> = (args) => <{{pascalCase name}} {...args} />;
