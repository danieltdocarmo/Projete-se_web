import I{{pascalCase name}}Props from './types';
import { Container } from './styles';

const {{pascalCase name}}: React.FC<I{{pascalCase name}}Props> = ({ children, style, className }) => {
  return <Container {...{ style, className }}>{children}</Container>;
};

export default {{pascalCase name}};
